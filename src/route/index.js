const tracks = require('./tracks.route');
const cache = require('./cache.route');
const tileset = require('./tileset.route');
const dataset = require('./dataset.route');

module.exports = [tracks, cache, tileset, dataset];
